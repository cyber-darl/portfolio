To create new react project Open empty repository on git, then go to command line

run git init, to initialize a new repository
npm create vite latest to utilize the latest vite package
npm install to install package-lock.json, node_modules
git add .
git commit -m commit message
git branch -M main
git remote add origin https from the github
git push -u origin main
